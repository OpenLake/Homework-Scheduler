import { useForm, Controller } from 'react-hook-form';
import { Typography, TextField, Stack } from '@mui/material';

const GradeForm = () => {
	return (
		<Stack spacing={3}>
			<Typography variant="h4">Grade Form</Typography>
		</Stack>
	);
};

export default GradeForm;
